# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
azmft

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Fake /var directory used for local development
.var

# Environment
.env